/**
 * Con questa istruzione andiamo a definire la nostra applicazione angular a cui ci agganciamo per la definizione di directive, 
 * controller e services
 * 
 */
var myApp = angular.module('filmRental', []);
